We will use Console.ReadLine() to get user input.
At line 13, We converted string input to int using Convert.ToItn32() method.
At line 15, We did addition of two integers in separate bracket. because --> string + int = string.
So, we put brackets, Like --> string + (int + int).
